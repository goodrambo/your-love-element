---
kind: backlog
status: active
last_reviewed: 2026-08-06
review_after: 2026-08-07
---

# Active Backlog

This is the only active task list. History files may contain old “next tasks”; those are not actionable unless copied here after revalidation.
## Sequential growth objective

- [x] Create the phased growth-control heartbeat: every 30 minutes while bootstrap/high-impact work remains, every four hours only after seven complete aggregate days and a 24-hour empty ready queue, and daily after Stage 1 completes and the first qualifying Stage 2 streak day. Require one non-repetitive verified result per run, review early-stage strategy every six runs, and keep the hard review date non-extendable.
- [x] Enforce exact `your-love-element` isolation in the repository contract, Harness, runbook, and sole `yle` heartbeat; allowlist only this filesystem/Git root, Supabase project, GitHub repository, Cloudflare Worker/zone, and Resend domain/addresses while denying every sibling asset in the same accounts.
- [x] Change the active Stage 1 objective to `30` consecutive final GSC `web` days for exact property `sc-domain:yourloveelement.com`, each strictly above `1,000` clicks (`>= 1,001`). Keep impressions diagnostic-only, use GSC's `America/Los_Angeles` daily boundary, leave the unspecified Stage 1 deadline `null`, and gate the original purchase objective behind Stage 1. The evaluator passes 20/20 contract, boundary, and transition tests; the Stage 1-to-2 bootstrap now explicitly requires `scorecard_read`, not the completed `gsc_read` gate, and the combined focused evaluator/fetcher/adapter suite passes 31/31. PR [#88](https://github.com/goodrambo/your-love-element/pull/88) merged as `d977ca6`; main Harness run `30939472162`, Pages run `30939470638`, and post-merge production health passed.
- [x] Complete the Stage 1 GSC data/authority gate: on 2026-08-04 the signed-in exact property supplied six contiguous final `web` days through 2026-08-02, the aggregate input passed unchanged through the evaluator with `gsc_read: true`, and the constraint moved from `access` to `traffic`. Exact-repository GitHub/Pages release access and low-risk site conversion/SEO authority remain connected; Lemon Squeezy and Meta are deliberately deferred and paid media remains `$0`.
- [x] Verify signed-in Supabase, Cloudflare, and Resend dashboard sessions; confirm the target Supabase project; configure and validate the project-scoped read-only Supabase MCP with schema and aggregate-only queries on 2026-07-30.
- [x] Complete the current Stage 1 baseline. Exact-property final `web` Performance now covers eight contiguous days from 2026-07-28 through 2026-08-04: every day has `0` clicks, while 2026-08-04 has the first and only recorded impression; the latest final day lagged by two days. Impressions remain diagnostic-only and do not qualify as clicks or visits. The first complete closed Taipei business day, 2026-07-30, had `3` landing sessions and `0` CTA clicks, quiz starts, previews, checkouts, or verified purchasers, but later Stage 2 business aggregates remain unavailable; do not infer zeros. Settled revenue, spend, CAC, and project-scoped Resend delivery aggregates remain unavailable.
- [x] Implement and locally test the protected aggregate growth scorecard/RPC; it exposes counts only and no customer identifiers or private report data.
- [x] Implement and locally test privacy-minimized first-party funnel/UTM aggregates; browser `purchase` is rejected and no contact, reading, answer, URL, referrer, IP, or user-agent data is stored.
- [x] Implement and test the deterministic aggregate decision evaluator for rolling windows, milestone gaps, constraint selection, authority gating, and experiment promotion/stopping.
- [x] Add a Harness-validated standing-authority contract with explicit-evidence requirements, default-denied production mutations, and `$0` paid-media caps; make the decision CLI enforce it.
- [ ] Complete Stage 1 with 30 consecutive final GSC days at `>= 1,001` web clicks. Then begin Stage 2: 10 verified non-refunded purchasers per Asia/Taipei day for 30 consecutive days. The 2026-10-15 hard review remains fixed; if Stage 1 is still incomplete, report it as the blocking missed stage rather than skipping it.

## Ready
- [x] Release a technical SEO regression that requires every indexable configured page to receive a crawlable same-origin link from another indexable page. PR [#90](https://github.com/goodrambo/your-love-element/pull/90) merged as `e8a993d`; the 30/30 focused SEO suite, main Harness run `30946279105`, Pages run `30946277650`, and post-merge live Harness passed. The current seven-URL cluster has no orphan page, while HTML, runtime, sitemap, paid flow, and the active Copy link experiment remain unchanged.
- [x] Release SEO freshness, sitemap inventory, and canonical schema-graph regressions. PR [#93](https://github.com/goodrambo/your-love-element/pull/93) merged as `fbe5570`, PR [#95](https://github.com/goodrambo/your-love-element/pull/95) as `dd6277d`, and PR [#99](https://github.com/goodrambo/your-love-element/pull/99) as `2a91f2b`; the latest 32/32 focused SEO suite, main Harness run `30966935277`, Pages run `30966934869`, and post-merge 5/5 public health gates passed. Pages run `30967117362` proved its Node.js 20 warning is from GitHub's platform-managed dynamic workflow, not repo source; the self-managed Harness already uses checkout v5. No ready project mutation remains for the successful warning: reopen only if a repo-scoped upgrade appears or Pages fails.
- [x] Release the followable inbound-link regression. PR [#103](https://github.com/goodrambo/your-love-element/pull/103) merged as `b1d5b21`; the 33/33 focused SEO suite, main Harness run `30977165171`, Pages run `30977164421`, and post-merge 5/5 public health gates passed. The release excludes `rel=nofollow` and download anchors from satisfying the only crawlable inbound path to an indexable page while leaving HTML, sitemap, runtime, tracking, paid flow, and the active Copy link experiment unchanged.
- [x] Release the robots root-block regression through PR [#104](https://github.com/goodrambo/your-love-element/pull/104) and merge `e3358c2`. Hypothesis: rejecting a wildcard `Disallow: /` prevents an accidental whole-site crawl blockade from silently eliminating search exposure while the Stage 1 baseline is still zero. Primary metric: the focused SEO suite passes 34/34 and distinguishes whole-site wildcard blocks from named-agent and path-only exclusions. Guardrail: no `robots.txt`, HTML, sitemap, runtime, tracking, paid-flow, or active Copy link experiment change. Main Harness run `30982086079`, Pages run `30982085217`, and post-merge 5/5 public health gates passed.
- [x] Release the sitemap robots-meta regression through PR [#105](https://github.com/goodrambo/your-love-element/pull/105) and merge `5562e38`. Hypothesis: rejecting `noindex`, `nofollow`, or duplicate robots meta tags on sitemap pages prevents an accidental page-level indexing/crawl blockade from suppressing search discovery while the Stage 1 baseline is zero. Primary metric: the focused SEO suite passes 35/35 across all seven sitemap URLs. Guardrail: no HTML, `robots.txt`, sitemap, runtime, tracking, paid-flow, or active Copy link experiment change. Main Harness run `30985710926`, Pages run `30985709966`, and post-merge 5/5 public health gates passed.
- [x] Release the canonical-route normalization regression through PR [#106](https://github.com/goodrambo/your-love-element/pull/106) and merge `4fb9351`. Hypothesis: rejecting credentials, ports, queries, fragments, or route-mismatched canonicals prevents accidental duplicate consolidation and index-signal dilution while the Stage 1 click baseline is zero. Primary metric: the focused SEO suite passes 36/36 across all eight configured pages. Guardrail: no HTML, canonical markup, sitemap, runtime, tracking, paid-flow, or active Copy link experiment change. Main Harness run `30989890915`, Pages run `30989890313`, and post-merge 5/5 public health gates passed.
- [x] Release the sitemap meta-refresh regression through PR [#107](https://github.com/goodrambo/your-love-element/pull/107) and merge `e264060`. Hypothesis: rejecting client-side redirect directives on sitemap pages prevents crawler redirect signals from silently removing an intended acquisition URL while Stage 1 exposure is zero. Primary metric: the focused SEO suite passes 37/37 and detects case-insensitive directives in either attribute order across all seven sitemap pages. Guardrail: test-only; no HTML, sitemap, runtime, tracking, paid-flow, or active Copy link experiment change. Main Harness run `30998366333`, Pages run `30998365417`, and post-merge 5/5 public health gates passed.
- [ ] Complete the test-only crawl-discovery safeguard release through draft PR [#109](https://github.com/goodrambo/your-love-element/pull/109), which now contains search-snippet eligibility test head `47302e9` plus docs-only status follow-ups and remains open, draft, mergeable, and successful on renewed PR Harness run `31081834098`; resolve its moving head live before any mutation. Hypothesis: rejecting standard and Googlebot-specific indexing or text-snippet blocks, requiring at least two crawlable inbound sources per indexable page, preserving one coherent main heading outline, and preventing editorial review-ownership/date drift protect discovery and search credibility while the Stage 1 click baseline is zero. Primary metric: the focused SEO suite passes `42/42`, including `none`, `nosnippet`, and `max-snippet:0` fixtures, both editorial Article author/publisher references, WebPage/Article dates, and visible review-date parity. Guardrail: no HTML, sitemap, runtime, tracking, paid-flow, or active Copy link experiment change. Remaining gate: explicit authorization for the exact protected-main merge before Pages and production smoke; stop on false positives, source-scope expansion, CI/deploy failure, or production breakage.
- [ ] After the active Copy link experiment reaches its existing seven-day gate or a strategy review replaces it, prepare one compatibility-examples and relationship-repair section on `/five-elements-love-compatibility/` for the five uncovered regulating/friction pairs—Wood–Earth, Wood–Metal, Fire–Metal, Fire–Water, and Earth–Water—without rewriting the five existing generating-cycle examples or starting a second primary experiment meanwhile. Hypothesis: distinct, search-matched conflict and repair examples can generate final property-level web clicks from the current one-impression, zero-click baseline without near-duplicate element pages. Primary metric: final property-level GSC web clicks over the first seven post-release final days. Guardrails: one primary experiment, preserve the existing `5/10` explicit pair coverage, no paid-flow/tracking/legal-claim change, no duplicate content, and production health. Sample/time gate: 7/14 final post-release days; stop on breakage, replace after 14 final days without click improvement, or reclassify to indexing reliability when the aggregate Page indexing report finishes processing and exposes an indexability constraint. Completion ETA remains unknown.
- [ ] After the active Copy link gate and draft PR #109 are resolved, prepare a typed Brand node for the homepage Product schema and update the canonical graph regression; do not combine it with the queued content experiment or current PR. Hypothesis: replacing the Product `brand` reference that resolves to Organization with a valid Brand node removes the exact-property Merchant listings brand-type warning while preserving one valid item. Primary metric: Merchant listings remains at one valid and zero invalid items after reprocessing, without the brand-type warning. Guardrails: no visible copy, tracking, paid flow, checkout, refund-policy, legal claim, `hasMerchantReturnPolicy`, or `shippingDetails` change. Sample/time gate: focused SEO regression, Harness, required offline browser/digest gates, reversible PR/CI/Pages, production smoke, then GSC aggregate reprocessing; stop on an invalid item, graph-reference drift, policy-scope expansion, or production breakage.

## Stage 2 later
- [ ] Restore one exact-project purchase aggregate read path to the `yle` runtime before Stage 2. Prefer `JOB_RUNNER_SECRET` for the protected scorecard or restore only the allowlisted Supabase aggregate reader. Do not expose secrets or customer/session/order/reading identifiers, and do not substitute frontend/GSC events for purchase truth.
- [x] Release the protected scorecard fetch-contract repair through reversible PR [#79](https://github.com/goodrambo/your-love-element/pull/79) and merge `139d093`. Hypothesis: closing the actual fetcher-to-Worker schema gap lets a future authorized secret bootstrap reach aggregate evaluation instead of rejecting the Worker's existing `generated_at` field. Primary metric: the 6/6 fetcher suite drives the exact fetcher through the real Worker handler and both aggregate RPC mocks for the requested closed-day range. Guardrail: only a valid millisecond-precision UTC ISO timestamp is accepted; missing/invalid timestamps, unknown/sensitive fields, cache/CORS/date/auth failures remain rejected; no Worker, RPC, migration, event, customer data, frontend, paid flow, report, email, spend, or active Copy link experiment change. Sample/time gate: local/CI/Harness, Pages/Worker builds, and production health/auth; stop on any schema broadening, non-mock request, or guardrail failure. All gates pass; the positive production bearer path remains blocked by unavailable `JOB_RUNNER_SECRET`.
- [x] Release the accepted-event-to-aggregate boundary regression through reversible PR [#77](https://github.com/goodrambo/your-love-element/pull/77) and merge `9f4ffd4`. Hypothesis: exact parity from the frontend allowlist through the database event constraint and scorecard RPC filters prevents a collector-accepted stage from silently disappearing from aggregate decisions. Primary metric: the 14/14 Worker suite requires the same 11 events in all three layers and no duplicate aggregate filter. Guardrail: no migration, RPC, event, runtime, customer data, paid flow, report, email, spend, or active Copy link experiment change. Sample/time gate: local/CI/Harness, Pages/Worker builds, and production health/auth; stop on vocabulary drift, missing/duplicate filters, source-scope expansion, or any guardrail failure. All gates pass.
- [x] Release frontend/Worker funnel-event boundary regression through reversible PR [#75](https://github.com/goodrambo/your-love-element/pull/75) and merge `dfb0517`. Hypothesis: exact allowlist parity plus real collector acceptance prevents a frontend/Worker name drift from silently erasing one or more first-party funnel stages. Primary metric: the 13/13 Worker suite requires the same 11 current funnel/share names in both source constants and HTTP 202 for every frontend name. Guardrail: no event, call site, runtime, customer data, paid flow, report, email, spend, or active Copy link experiment change. Sample/time gate: local/CI/Harness, Pages/Worker builds, and production health/auth; stop on set drift, any rejected name, or source-scope expansion. All gates pass.
- [x] Release the active share-attribution boundary regression through reversible PR [#73](https://github.com/goodrambo/your-love-element/pull/73) and merge `8c19223`. Hypothesis: reading the live `shareReferralUrl` and exercising its exact four UTM labels through the first-party collector prevents the Copy link experiment from silently becoming unmeasurable after frontend/Worker contract drift. Primary metric: the 12/12 Worker suite requires HTTP 202 and exact persistence of `share_card/referral/organic_share/result_card`. Guardrail: no runtime, event semantics, customer data, frontend, Worker behavior, paid flow, report, email, or spend change. Sample/time gate: local/CI/Harness, Pages/Worker builds, and production health/auth; stop on source drift, rejected labels, unexpected fields, or any source-scope expansion. All gates pass; the strategy review retains the experiment without promotion or replacement until natural referral aggregates become available.
- [x] Release same-origin fragment-link regression coverage through reversible PR [#71](https://github.com/goodrambo/your-love-element/pull/71) and merge `c8f9e992`. Hypothesis: resolving each fragment link against the configured canonical page map prevents a renamed, missing, duplicate, or cross-route target from silently breaking acquisition/navigation anchors while file-level link checks remain green. Primary metric: the 29/29 SEO suite requires every same-origin fragment to target a configured page and exactly one matching ID, including `/#reading`, `#preview`, and `#unlockReport`. Guardrail: HTML, CSS, runtime JavaScript, tracking, active Copy link experiment, API, checkout, paid flow, report, and email behavior unchanged. Sample/time gate: local/CI/Harness, Pages, and production health/auth before completion; stop on a false target mapping, source-scope expansion, or any guardrail failure. All gates pass.
- [x] Establish the first processed Search Console Performance and homepage indexability baseline for exact property `sc-domain:yourloveelement.com`. Performance was updated about 4.5 hours before the 2026-08-02 inspection and reported `0` clicks, `0` impressions, and no query rows for 2026-07-28 through 2026-07-31; exact homepage inspection separately confirmed indexed status, successful Googlebot Smartphone fetch on 2026-07-31, allowed crawl/index directives, and matching declared/selected canonicals. Decision: rule out homepage indexability as the cause of zero exposure, retain the active Copy link referral experiment without promotion or replacement because no natural referral or funnel aggregate is available, keep `access` as the evaluator-selected primary constraint, avoid a redundant indexing request, and recheck after another closed search interval or indexing change. Guardrails: read-only exact-property evidence, no property enumeration, no query fabrication, no customer-facing change, and no interpretation of zero exposure as conversion evidence.
- [x] Release social-preview metadata plus asset-integrity regression coverage through reversible PRs [#65](https://github.com/goodrambo/your-love-element/pull/65) and [#68](https://github.com/goodrambo/your-love-element/pull/68), merges `f0e8485` and `7fd971d`. Hypothesis: CI protection for the three core acquisition pages and referenced PNG prevents Open Graph/Twitter metadata or file drift from silently weakening organic sharing while aggregate funnel truth is unavailable. Primary metric: the 28/28 SEO suite requires one complete metadata contract per page, canonical/`og:url` parity, cross-platform title/image/alt agreement, a valid PNG/IHDR `1200 x 630` asset, and a 200 KB transfer budget. Guardrail: asset bytes, HTML, frontend runtime, tracking, active referral experiment, API, checkout, Worker, paid flow, report, and email behavior unchanged. Sample/time gate: local/CI/Harness, Pages, production 117,141-byte asset, and health/auth; stop on duplicate/missing metadata, invalid format, URL/dimension/type drift, budget breach, or any source-scope expansion. All gates pass.
- [x] Release a full-option keyboard focus indicator for free-quiz radios through reversible PR [#63](https://github.com/goodrambo/your-love-element/pull/63) and merge `3606b6c`. Hypothesis: expanding focus visibility from the 18px control to the 48–52px choice card makes keyboard location easier to perceive without changing the active referral experiment. Primary metric: the focused card exposes a 3px outline, 3px offset, and non-empty shadow at desktop and `390 x 844`. Guardrail: answers, pointer auto-advance logic/timing, validation, preview, share/referral, tracking, API, checkout, payment, webhook, report, and email behavior unchanged. Sample/time gate: regression-first 26/26 SEO suite, digest-bound desktop/mobile offline Browser checks, Harness/CI, Pages, production source/browser, and health/auth; stop on missing focus, selection or flow drift, API activity, overflow, console errors, or any guardrail failure. All gates pass; paid-flow E2E remains a digest-scoped `SKIPPED` waiver, not proof.
- [x] Release free-quiz control-level validation semantics through reversible PR [#61](https://github.com/goodrambo/your-love-element/pull/61) and merge `2d58e3a`. Hypothesis: exposing invalid state and the existing error description on the unresolved control reduces assistive-technology error-recovery ambiguity without changing the active referral experiment. Primary metric: all unanswered radios, or only the missing month/day control, expose `aria-invalid=true` plus `aria-describedby=quizValidation`, and both clear after correction. Guardrail: validation decisions/copy/focus, question flow, paid signals, preview, share/referral, tracking, API, checkout, payment, webhook, report, and email behavior unchanged. Sample/time gate: regression-first 25/25 SEO suite, digest-bound `1280 x 720` and `390 x 844` radio/month/day/full-quiz smoke, Harness/CI, Pages, production source/browser, and health/auth; stop on wrong/stale semantics, duplicate announcements, API activity, overflow, or any guardrail failure. All gates pass; paid-flow E2E remains a digest-scoped `SKIPPED` waiver, not proof.
- [x] Release active-question focus after every non-final free-quiz forward transition through reversible PR [#59](https://github.com/goodrambo/your-love-element/pull/59) and merge `d3c2182`. Hypothesis: focusing the selected answer or first unresolved control after Back then Continue reduces keyboard and screen-reader navigation loss without changing the active referral experiment. Primary metric: manual forward navigation to a saved Q2 focuses its selected intent answer, while an unanswered target focuses its first control. Guardrail: answers/order, Back, validation, radio auto-advance timing, preview, share/referral, tracking, API, checkout, payment, webhook, report, and email behavior unchanged. Sample/time gate: regression-first 24/24 SEO suite, digest-bound `1280 x 720` and `390 x 844` saved/unanswered/full-quiz smoke, Harness/CI, Pages, production source/browser, and health/auth; stop on lost/incorrect focus, a focus loop, API activity, overflow, or any guardrail failure. All gates pass; paid-flow E2E remains a digest-scoped `SKIPPED` waiver, not proof.
- [x] Release an assistive-technology announcement for asynchronous share feedback through reversible PR [#43](https://github.com/goodrambo/your-love-element/pull/43) and merge `9e10b74`. Hypothesis: announcing share, download, and copy results prevents silent uncertainty for screen-reader users without changing the active referral experiment. Primary metric: one polite, atomic status region updates to `Referral link copied.` after Copy link. Guardrail: referral URL/events, card image, quiz, API, checkout, tracking, and paid-flow behavior unchanged. Sample/time gate: 17/17 SEO regression, digest-bound `1280 x 900` full-quiz/share-card smoke and `390 x 844` layout check, Harness/CI, Pages, and production source/health; stop on missing/duplicate feedback, overflow, API activity, private parameters, or any guardrail failure. All gates pass.
- [x] Release route-correct current-page navigation semantics through reversible PR [#41](https://github.com/goodrambo/your-love-element/pull/41) and merge `b38d6f1`. Hypothesis: a consistent content-cluster navigation with one machine-readable current location improves orientation for screen-reader and keyboard users without changing the active referral experiment. Primary metric: exactly one `aria-current="page"` whose path matches each core page. Guardrail: no overflow, mobile-wrap, quiz, API, tracking, or paid-flow regression. Sample/time gate: SEO regression, digest-bound `1280 x 900` and `390 x 844` browser smoke on all three routes, Harness/CI, Pages, and production source/health before completion; stop on a missing, duplicate, or wrong-route marker or any guardrail failure. SEO 16/16, local/merge Harness, Pages, published source, site/Worker health, and unauthenticated scorecard gates pass.
- [x] Release deferred loading and intrinsic layout space for the 314 KB below-fold report banner through reversible PR [#39](https://github.com/goodrambo/your-love-element/pull/39) and merge `00c540b`. Hypothesis: keeping the sample image out of the first viewport's resource set reduces initial competition and prevents image layout shift without changing the active referral experiment. Primary metric: empty `currentSrc` and no Earth-banner page asset before scrolling, then exactly one complete 1440-pixel asset after scrolling. Guardrail: no missing image, overflow, console/API, quiz, tracking, or paid-flow regression. Sample/time gate: SEO regression, digest-bound `1280 x 900` and `390 x 844` browser smoke, Harness/CI, Pages, and production smoke before completion; stop on an eager/missing/duplicate image or any guardrail failure. SEO 15/15, local/merge Harness, Pages, published source, site/Worker health, and unauthenticated scorecard gates pass.
- [x] Release earlier discovery of the existing above-the-fold hero WebP through reversible PR [#37](https://github.com/goodrambo/your-love-element/pull/37) and merge `89bb655`. Hypothesis: one high-priority head preload matching the hero image URL reduces LCP resource-discovery delay without changing the active referral experiment. Primary metric: exactly one preload with the same URL as the complete hero image, plus one observed hero asset/request in offline QA. Guardrail: no duplicate request, overflow, console/API, quiz, tracking, or paid-flow regression. Sample/time gate: SEO regression, digest-bound `1280 x 900` and `390 x 844` browser smoke, Harness/CI, Pages, and production smoke before completion; stop on duplicate download or any guardrail failure. SEO 14/14, local/merge Harness, Pages, published source, site/Worker health, and unauthenticated scorecard gates pass.
- [x] Release an organic authority-gate correction through reversible PR [#35](https://github.com/goodrambo/your-love-element/pull/35) and merge `dddb619`. Hypothesis: limiting the access action and 2026-08-02 authority milestone to the one required aggregate path, `scorecard_read`, prevents deliberately excluded Meta, paid-media, paid-E2E, social-publish, and optional provider permissions from causing a recurring false bootstrap blocker. Primary metric: the missing-scorecard action reports only `scorecard_read`, while the complete authority inventory continues to expose every unavailable capability. Guardrail: no authority is granted, hidden, inferred, or made usable; standing-authority intersections, paid-media `$0`, scorecard privacy, customer data, and production behavior remain unchanged. Sample/time gate: evaluator regressions plus Harness/CI before release; stop on any authorization broadening, loss of unavailable-capability reporting, or changed business metric calculation. Evaluator 13/13, PR/merge Harness, Pages, published main source, site/Worker health, and unauthenticated scorecard gates pass.
- [x] Release shared reduced-motion accessibility through reversible PR [#33](https://github.com/goodrambo/your-love-element/pull/33) and merge `cc709ba`. Hypothesis: honoring the operating-system reduced-motion preference removes unnecessary animation and smooth scrolling for motion-sensitive visitors without changing the active referral experiment. Primary metric: the shared stylesheet has one deterministic `prefers-reduced-motion: reduce` rule that reduces animation/transition duration and iteration and restores automatic scrolling. Guardrail: no quiz, tracking, checkout, payment, webhook, report, email, Worker, or paid-flow behavior changes. Sample/time gate: CSS regression coverage plus offline desktop/mobile browser smoke before release; stop on navigation, layout, quiz progression, console/API, cache, or paid-flow regression. SEO 14/14, Harness, Pages, production cache/CSS, site/Worker health, and unauthenticated scorecard gates pass. In-app browser smoke at 1280x900 and 390x844 had no overflow, console error, or API request; the browser's OS preference remained in its normal no-preference state, while the automated test covers the reduced-preference rule.
- [x] Release an exact-origin protected scorecard fetcher for the scheduled runtime through reversible PR [#31](https://github.com/goodrambo/your-love-element/pull/31) and merge `2f1235b`. Hypothesis: once `JOB_RUNNER_SECRET` is injected, a single fail-closed command can retrieve the latest aggregate without exposing the credential, accepting another Worker, or persisting customer-level data. Primary metric: five collector tests cover exact URL/header behavior, pre-request validation, single-attempt `401`/`404`, privacy schema rejection, and private/no-store non-CORS response enforcement. Guardrail: aggregate JSON only; the fetcher does not evaluate, mutate production, retry credentials, accept a caller-selected origin, or log response bodies. Sample/time gate: use only after the secret is present and pass the output to the required growth-control CLI; stop on auth rejection, route absence, contract drift, CORS, cacheability, or sensitive fields. PR/merge Harness, Pages, site/Worker health, and unauthenticated scorecard gates pass.
- [x] Run the first post-midnight aggregate decision for 2026-08-01 with `scorecard: null` after confirming the scheduled runtime lacks both `JOB_RUNNER_SECRET` and the exact-project Supabase aggregate tool. Hypothesis: a missing latest closed day must fail closed instead of reusing 2026-07-30 or turning unknown purchases into zero. Primary metric: evaluator status `blocked_on_aggregate_truth` with constraint `access`. Guardrail: streak, purchase rates, CAC, and ROAS remain unknown; no customer data, secret, provider mutation, or inferred value. Sample/time gate: retry after one exact-project aggregate access path is exposed; stop on ambiguous project scope or secret disclosure.
- [x] Release an assistive-technology announcement for the auto-advancing free quiz through reversible PR [#28](https://github.com/goodrambo/your-love-element/pull/28) and merge `6df3170`: the visible step summary is now one polite, atomic status region. Hypothesis: announcing each automatic question transition reduces silent abandonment for screen-reader users. Primary metric: desktop/mobile browser interaction updates the region from Question 1 to Question 2 with the correct mood. Guardrail: visuals, quiz logic, tracking, and paid-flow digest unchanged; stop on duplicate announcements, overflow, console/API regression, or paid-flow drift. SEO 13/13, Harness/CI, Pages, production source, and site/Worker health/auth gates pass.
- [x] Release an always-available Copy link action for the existing privacy-safe share referral URL through reversible PR [#18](https://github.com/goodrambo/your-love-element/pull/18) and merge `f47e4ac`. Hypothesis: desktop and no-WebShare visitors will have a usable referral path, increasing aggregate landing sessions with `utm_source=share_card`. Primary metric: naturally arriving aggregate `share_card` landing sessions; copying itself is not tracked because it does not prove sharing. Guardrail: the copied URL is the same fixed four-UTM URL with no private identifier, and checkout/paid-flow behavior remains unchanged. Gate: first naturally referred landing or seven closed days; stop on clipboard failure in supported browsers, a private parameter, share-card breakage, or production regression. Exact clipboard, desktop/mobile layout, Node/Harness, PR CI, Pages, production source, and Worker health/auth gates pass; no natural referred landing is claimed yet.
- [x] Release the privacy-safe share attribution change through reversible PR [#16](https://github.com/goodrambo/your-love-element/pull/16) and merge `8cf9210`: replace the untagged homepage link in share text with fixed aggregate UTM labels, keep the image share payload unchanged, and production-smoke the exact cache revision. Hypothesis: attributable shared links will expose referral landing sessions without private reading data. Primary metric: aggregate landing sessions with `utm_source=share_card`. Guardrail: no answer, element, email, session, reading, order, or customer identifier in the URL. Gate: first naturally referred landing or seven closed days; stop on any private parameter, share-card breakage, or production regression. Regression tests, desktop/mobile offline browser QA, Harness/Pages CI, production source, and Worker health/auth smoke pass; no natural referred landing is claimed yet.
- [x] Add a deterministic aggregate adapter and contract test that converts the two service-role-only RPC result sets into evaluator-ready daily input, including landing/full-report merging, strict closed-day completeness, sanitized UTM validation, and rejection of identifiers, unknown fields, and customer-level fields.
- [x] Add a golden parity test that feeds the same aggregate RPC fixtures through the protected Worker route and local adapter, requires identical source/privacy/limitations, range, goal/streak, totals, days, and attribution, locks deterministic multi-UTM ordering, and makes the Worker reject non-object, missing, or unknown aggregate fields, incomplete commerce rows, and duplicate or out-of-range funnel attribution rows without using production data.
- [x] Align homepage WebPage/Product structured-data images and the image sitemap with the deployed 112,840-byte WebP through reversible PR [#7](https://github.com/goodrambo/your-love-element/pull/7); add regression coverage, require Harness/Pages success, and complete production source plus desktop/mobile smoke on merge `110435e`.
- [x] Optimize the duplicated homepage hero transfer through reversible PR [#5](https://github.com/goodrambo/your-love-element/pull/5): replace the 2,268,306-byte PNG requests with a 112,840-byte WebP, add layout/loading hints and a 200 KB regression budget, require Harness/Pages success, and complete production desktop/mobile smoke on merge `e270c6f`.
- [x] Release the verified conversion audit through reversible branch `codex/preview-offer-clarity` and PR #2; require Harness/CI success, merge/deploy only the allowlisted site, and complete production desktop/mobile plus SEO schema smoke on merge `c468bf7`.
- [x] Implement the initial SEO/GEO/AEO foundation: answer-first homepage content, Five Elements pillar guide, methodology/AI disclosure page, internal cluster links, schema, sitemap updates, and automated regression coverage.
- [x] Browser-smoke the homepage and both search content pages at desktop and `390 x 844`; confirm no horizontal overflow or console warnings/errors, complete the 10-question Earth preview, and keep the local full-report page payment-locked.
- [x] Review the Five Elements guide and methodology copy for brand voice and cultural framing before publication; the user approved both on 2026-07-30.
- [x] Functionally review the unpublished landing redesign at desktop and `390 x 844` mobile sizes; no horizontal overflow or console warnings/errors on 2026-07-30.
- [x] Smoke-test all 10 free questions, Earth preview/share-card generation, localhost offline mode, first-party collection isolation, and local paid-page blocking on 2026-07-30.
- [x] Complete the user's visual/brand review of the redesign. The user approved it on 2026-07-30; share-card generation and the download success state were browser-verified at `1080 x 1350` without console warnings/errors.
- [x] Review the full verified source scope, commit it as `b9eff29`, push `codex/verified-site-seo-release`, and open draft PR [#1](https://github.com/goodrambo/your-love-element/pull/1) without changing `main` or production.
- [x] Review PR #1 and merge/publish with explicit authorization. Release merge `0de5835` reached `main`; the Harness and Pages workflows both passed on 2026-07-30.
- [x] After the authorized frontend release, validate the two new URLs in production, submit the sitemap in Search Console, inspect their indexing state, and begin the 30/60/90-day organic baseline in `docs/SEO_GEO_AEO_PLAN.md`. The sitemap succeeded with seven discovered URLs; both new pages are discovered but not yet indexed.
- [x] After the measurement deployment, verify exact Pages commit/assets, four Worker health endpoints, browser console, cache revision, auth/origin/event rejection, and allowlisted analytics ingestion.

## Authorized measurement release

- [x] Apply superseding `202607290002_add_growth_scorecard_function.sql`, then `202607300001_add_first_party_funnel_events.sql`, to the intended Supabase project. The checksum-protected `202607290001` attempt was parser-rejected before creating objects; both effective functions now exist with service-role-only execute, RLS is enabled, and the funnel table had zero rows at verification.
- [x] Deploy only the authorized measurement Worker/frontend scope as commit `6005da3`; verify unauthenticated scorecard HTTP 401, untrusted-origin HTTP 403, frontend `Purchase` HTTP 400, allowlisted production browser ingestion, and aggregate Supabase receipt. The positive bearer-secret route remains unit-tested but was not called in production because the secret was not exposed.

## Still requires separate authorization
- [ ] Run one end-to-end checkout/webhook/eight-signal/report/email test.
- [ ] Confirm the new report is delivered once, the reading reaches `delivered`, and no duplicate job/email appears.
- [ ] Confirm Meta `Purchase` in Events Manager if tracking is in scope.

## Requires fresh business data

- [x] Collect the first complete current funnel count from landing view through Purchase: 2026-07-30 closed with `3` landing sessions and `0` CTA clicks, quiz starts, previews, checkouts, or verified purchasers. Continue the seven-day baseline under the main objective item before choosing a customer-facing experiment.
- [ ] Validate the revised preview-to-offer experience with 5-8 target users who read US/UK English; record quiz comprehension, perceived personalization, price clarity, purchase intent, and the exact objections that stop checkout.
- [ ] Add customer proof only after authentic buyer feedback and explicit publication consent are available; do not fabricate testimonials, star ratings, buyer counts, or outcome claims.
- [ ] Define a concrete refund eligibility window and decision timeline with business/legal review, then surface the approved rule before checkout; the current policy is intentionally case-based and may leave cautious buyers uncertain.
- [ ] Confirm which June social items were actually published and record their performance.
- [ ] Decide the next content or paid-acquisition experiment from current data, not the May/June historical snapshot.

## Maintenance

- [x] Commit and push the reviewed Harness files on draft PR #1, then merge it so the default branch, fresh clones, and remote `harness` workflow receive the Harness.
- [ ] After the final hook files are committed, review and trust their exact hash with `/hooks`; repeat whenever a hook changes.
- [ ] After the active Codex capture lifecycle is finished, audit `.git` special refs and propose a safe, separately approved cleanup; do not prune automatically.
- [ ] Enable the GitHub `harness` workflow as a required branch-protection check if repository policy should block direct merges.
